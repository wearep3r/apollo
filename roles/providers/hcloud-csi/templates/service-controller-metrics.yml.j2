---
apiVersion: v1
kind: Service
metadata:
  name: {{ hcloud_resource_prefix }}-csi-controller-metrics
  namespace: {{ hcloud_namespace }}
  labels:
    app: {{ hcloud_resource_prefix }}-csi
spec:
  selector:
    app: {{ hcloud_resource_prefix }}-csi-controller
  ports:
    - port: 9189
      name: metrics
      targetPort: metrics